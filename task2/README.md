## Usage

Create the generator by passing `a` (base) and `n` (number of powers).

Iterate over the object to get consecutive powers of `a`.

Example:

```python
for x in Powers(2, 5):
    print(x)
```
